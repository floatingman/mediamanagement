# Generate with: k8s/scripts/gen-env.sh (pulls from the repo .env)
# or copy to env.txt and fill in. env.txt is GITIGNORED.
POSTGRES_PASSWORD=<.env LINKWARDEN_DB_PASSWORD>
DATABASE_URL=postgresql://postgres:<password>@linkwarden-db:5432/postgres
NEXTAUTH_SECRET=<.env LINKWARDEN_NEXTAUTH_SECRET>
NEXTAUTH_URL=https://linkwarden.thenewmans.casa/api/v1/auth
MEILI_MASTER_KEY=<.env LINKWARDEN_MEILI_MASTER_KEY>
# Ollama is GPU-bound and stays on the Docker VM (192.168.0.9) permanently —
# the cluster nodes have no NVIDIA GPUs:
NEXT_PUBLIC_OLLAMA_ENDPOINT_URL=http://192.168.0.9:11434
OLLAMA_MODEL=qwen3.5:4b
