# Generate with k8s/scripts/gen-env.sh. GITIGNORED.
# Container-side names; mapping from .env noted per key. One shared secret —
# each deployment ignores keys it doesn't consume.
NEXTDASH_WRITE_TOKEN=<.env NEXTDASH_WRITE_TOKEN>
JWT_SECRET=<.env CONVERTX_JWT_SECRET — convertx>
POSTGRES_PASSWORD=<.env ZIPLINE_DB_PASSWORD — zipline-db>
DATABASE_URL=postgres://zipline:<password>@zipline-db:5432/zipline
CORE_SECRET=<.env ZIPLINE_CORE_SECRET — zipline>
