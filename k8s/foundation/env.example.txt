# Copy to env.txt (or generate from the repo .env with k8s/scripts/gen-env.sh).
# env.txt is GITIGNORED — real values never get committed.
CF_DNS_API_TOKEN=<cloudflare API token, Zone:DNS:Edit — .env CLOUDFLARE_DNS_TOKEN>
ACME_EMAIL=<Let's Encrypt expiry-notice email — .env ACME_EMAIL>
