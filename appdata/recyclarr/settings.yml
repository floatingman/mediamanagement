# yaml-language-server: $schema=https://raw.githubusercontent.com/recyclarr/recyclarr/v8.7.2/schemas/settings-schema.json

# v8: official TRaSH Guides + config-templates are used automatically
# (version-aware branch selection). The old `repositories:` pin — needed
# because v7 resolves include templates via includes.json, removed upstream
# 2026-08-07 — is obsolete: v8 has no official include templates at all.
# v7 rollback bundle: media-k8s-3:/var/tmp/recyclarr-config-backup-20260922.tgz
